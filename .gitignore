# Generated by `node setup.js`: the app's own copies of Node and OrcaSlicer,
# about 240 MB. Rebuildable on any machine, and AGPL-licensed — see
# vendor/README.md.
/vendor/*
!/vendor/README.md

# Release archives, built by `node make-release.js`, and any split parts of
# them made for transfer. Rebuildable, and they carry the bundled AGPL
# binaries — see vendor/README.md.
/dist/*.zip
/dist/*.zip.part*

node_modules/
result.json
